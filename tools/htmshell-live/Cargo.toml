[package]
name = "htmshell-live"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[features]
default = []
gpu-renderer = ["htm-shell-host/gpu-renderer"]

[dependencies]
htm-shell-host = { path = "../../crates/htm-shell-host" }
